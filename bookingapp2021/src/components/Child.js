import React from 'react'

function Child({children}) {
    return (
        <div>
             {children}
             this is the child element
             
        </div>
    )
}

export default Child
